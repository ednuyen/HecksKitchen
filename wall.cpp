#include "wall.h"
Wall::Wall(QWidget *parent)
    : QWidget(parent) {
}

void Wall:: paintEvent(QPaintEvent* e) {
//    QPainter painter(this);
//    QPixmap player(":/wall.png");
//    player = player.scaled(this->size(), Qt::IgnoreAspectRatio);
//    painter.drawPixmap(rect(), player);
    return;
}
